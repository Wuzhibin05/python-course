主要实现以下功能

1，将用户信息存入在json文件中。
2，读取用户信息到内存中。
3，判断用户登录信息。判断：用户不存在；用户已锁定；用户密码错误。
4，每个用户只能输入最多可尝试三次，三次输入错误退出程序，且将锁定标志写入到程序  ；用户只要输入对了一次密码，退出程序且锁定标识置零；多个用户连续输入，只要不超过三次。用户之间互相不会影响自己的锁定记录（比如alex用户输入的2次，中途再输入其他用户信息，然后第三次输错alex用户密码，此用户还是被锁定）
5，整个循环的退出用退出标志来控制。
